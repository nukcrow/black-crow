name: Update Personal Subscription

on:
  schedule:
    - cron: '0 */4 * * *'  # اجرای هر ۴ ساعت جهت تازه‌سازی کانفیگ‌ها
  workflow_dispatch:        # قابلیت اجرای دستی

jobs:
  update-subscription:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests

      - name: Run Switcher Script
        run: python switcher.py

      - name: Commit and Push to Main
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          
          git add sub.txt
          git diff --quiet && git diff --staged --quiet || (git commit -m "Auto-update fresh configs [$(date -u +'%Y-%m-%d %H:%M UTC')]" && git push origin main)
